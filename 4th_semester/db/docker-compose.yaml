services:
  db_postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: root
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
