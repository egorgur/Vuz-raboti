"""
Пакет auth/ — компонент аутентификации.

Принцип общего замыкания (CCP): все классы, которые изменяются
по одним и тем же причинам (изменение способа аутентификации),
сгруппированы в одном пакете.

Принцип общего повторного использования (CRP): если клиент
использует auth.router, он неявно зависит только от auth-компонента,
а не от notes/ или других несвязанных модулей.
"""
