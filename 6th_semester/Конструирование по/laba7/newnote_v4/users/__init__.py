"""
Пакет users/ — компонент управления пользователями.

Принцип общего замыкания (CCP): модель User, репозиторий доступа к данным
и FastAPI-провайдеры зависимостей сгруппированы в одном пакете, потому что
изменяются по одной причине — изменение представления/хранения сущности
«пользователь». Ранее они находились в auth/ и были выделены, чтобы
аутентификация (auth/) и управление пользователями (users/) развивались
независимо.

Принцип общего повторного использования (CRP): клиент, которому нужна лишь
работа с пользователями (admin/, notes/), зависит от компонента users/,
а не от деталей аутентификации.

Принцип ацикличных зависимостей (ADP): users/ → interfaces/ (абстракции),
auth/ → users/, admin/ → users/. Циклов нет.
"""

from users.models import User
from users.repository import UserRepository

__all__ = ["User", "UserRepository"]
